version: 2
jobs:
  build:
    working_directory: /go/src/github.com/satishkumarsk/artifactorymock
    environment:
      TEST_RESULTS: /tmp/test-results
    docker:
    - image: circleci/golang:1.15
    steps:
    - checkout
    - run: go test ./...